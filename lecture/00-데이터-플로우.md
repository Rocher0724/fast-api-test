# 데이터 플로우 다이어그램: FE → 서버 → DB 에러 발생 지점

> 이 파일의 Mermaid 다이어그램은 GitHub에서 자동으로 렌더링됩니다.
> 로컬에서 미리보기하려면 VS Code의 "Markdown Preview Mermaid Support" 확장을 설치하세요.

---

## 전체 데이터 흐름과 에러 발생 지점

```mermaid
flowchart TB
    subgraph FE["🖥️ 프론트엔드 (브라우저)"]
        A[사용자 액션<br/>버튼 클릭, 폼 제출] --> B[JavaScript 실행<br/>이벤트 핸들러]
        B --> C[fetch 호출<br/>API 요청 생성]
        C --> D[요청 전송<br/>URL + Method + Body + Headers]
    end

    subgraph NET["🌐 네트워크"]
        D --> E{CORS 체크<br/>브라우저 보안 검사}
        E -->|허용| F[HTTP 요청 전달]
        E -->|차단| ERR_CORS[❌ CORS 에러<br/>Console에서 확인]
    end

    subgraph BE["⚙️ 백엔드 (FastAPI)"]
        F --> G[라우트 매칭<br/>URL + Method 확인]
        G -->|못 찾음| ERR_404[❌ 404 Not Found]
        G -->|메서드 불일치| ERR_405[❌ 405 Method Not Allowed]
        G -->|찾음| H[데이터 검증<br/>Pydantic 모델 체크]
        H -->|검증 실패| ERR_422[❌ 422 Unprocessable Entity<br/>Network Response에서 detail 확인]
        H -->|검증 통과| I[비즈니스 로직 실행<br/>데이터 처리]
        I -->|코드 버그| ERR_500[❌ 500 Internal Server Error<br/>서버 터미널 Traceback 확인]
        I -->|정상| J[DB 작업 요청]
    end

    subgraph DB["🗄️ 데이터베이스"]
        J --> K{DB 연결<br/>커넥션 풀에서 가져오기}
        K -->|연결 실패| ERR_CONN[❌ ConnectionError<br/>서버 로그에서 확인]
        K -->|풀 부족| ERR_POOL[❌ TimeoutError<br/>QueuePool limit reached]
        K -->|연결 성공| L[쿼리 실행<br/>SELECT / INSERT / UPDATE / DELETE]
        L -->|테이블 없음| ERR_TABLE[❌ UndefinedTable<br/>마이그레이션 필요]
        L -->|제약조건 위반| ERR_INTEG[❌ IntegrityError<br/>중복/NULL 위반]
        L -->|성공| M[결과 반환]
    end

    subgraph RESP["📨 응답 흐름"]
        M --> N[FastAPI가 JSON 응답 생성]
        N --> O[HTTP 응답 전송<br/>Status Code + Body]
        O --> P[브라우저가 응답 수신]
        P --> Q[JavaScript가 응답 처리<br/>response.json]
        Q --> R[화면에 데이터 표시<br/>DOM 업데이트]
    end

    ERR_CORS -.->|확인 장소| CC1["브라우저 Console 탭"]
    ERR_404 -.->|확인 장소| CC2["브라우저 Network 탭"]
    ERR_405 -.->|확인 장소| CC2
    ERR_422 -.->|확인 장소| CC2
    ERR_500 -.->|확인 장소| CC3["서버 터미널 / Render Logs"]
    ERR_CONN -.->|확인 장소| CC3
    ERR_POOL -.->|확인 장소| CC3
    ERR_TABLE -.->|확인 장소| CC3
    ERR_INTEG -.->|확인 장소| CC3

    style ERR_CORS fill:#ff6b6b,color:#fff
    style ERR_404 fill:#ff6b6b,color:#fff
    style ERR_405 fill:#ff6b6b,color:#fff
    style ERR_422 fill:#ff6b6b,color:#fff
    style ERR_500 fill:#ff6b6b,color:#fff
    style ERR_CONN fill:#ff6b6b,color:#fff
    style ERR_POOL fill:#ff6b6b,color:#fff
    style ERR_TABLE fill:#ff6b6b,color:#fff
    style ERR_INTEG fill:#ff6b6b,color:#fff
    style CC1 fill:#4ecdc4,color:#fff
    style CC2 fill:#4ecdc4,color:#fff
    style CC3 fill:#4ecdc4,color:#fff
```

---

## 요청(Request) 흐름 상세

```mermaid
sequenceDiagram
    participant User as 사용자
    participant Browser as 브라우저 (JS)
    participant Server as FastAPI 서버
    participant DB as 데이터베이스

    User->>Browser: 버튼 클릭
    Note over Browser: JS 이벤트 핸들러 실행

    Browser->>Browser: fetch() 호출
    Note over Browser: ❌ JS 에러 시 여기서 멈춤<br/>(Console 탭 확인)

    Browser->>Server: HTTP 요청 (POST /api/users)
    Note over Browser,Server: ❌ CORS 에러 시 브라우저가 차단<br/>(Console 탭 확인)

    Server->>Server: 라우트 매칭
    Note over Server: ❌ 404/405: URL이나 Method 불일치

    Server->>Server: 데이터 검증 (Pydantic)
    Note over Server: ❌ 422: 필드 누락, 타입 오류

    Server->>Server: 비즈니스 로직 실행
    Note over Server: ❌ 500: 코드 버그 (Traceback 확인)

    Server->>DB: 쿼리 실행
    Note over Server,DB: ❌ ConnectionError: DB 연결 실패<br/>❌ IntegrityError: 제약조건 위반

    DB-->>Server: 쿼리 결과 반환

    Server-->>Browser: HTTP 응답 (200 + JSON)
    Note over Browser: ❌ response.ok 체크 안 하면<br/>에러 응답도 처리 시도

    Browser->>Browser: 응답 데이터로 화면 업데이트
    Note over Browser: ❌ 필드명 불일치 시 undefined

    Browser-->>User: 화면에 결과 표시
```

---

## 에러 확인 장소 요약

```mermaid
flowchart LR
    subgraph 확인도구["에러 확인 도구"]
        T1["🔍 브라우저 Console"]
        T2["🔍 브라우저 Network"]
        T3["🔍 서버 터미널"]
        T4["🔍 FastAPI /docs"]
        T5["🔍 Render Logs"]
    end

    subgraph 에러유형["에러 유형"]
        E1["JS 에러, CORS 에러"]
        E2["404, 405, 422 + 응답 상세"]
        E3["500 Traceback, DB 에러"]
        E4["API 직접 테스트<br/>프론트/백 에러 분리"]
        E5["배포 빌드 에러, 런타임 에러"]
    end

    T1 --- E1
    T2 --- E2
    T3 --- E3
    T4 --- E4
    T5 --- E5

    style T1 fill:#4ecdc4,color:#fff
    style T2 fill:#45b7d1,color:#fff
    style T3 fill:#96ceb4,color:#fff
    style T4 fill:#ffeaa7,color:#333
    style T5 fill:#dfe6e9,color:#333
```

---

## 배포 환경에서의 데이터 흐름

```mermaid
flowchart TB
    subgraph LOCAL["💻 로컬 개발"]
        L1[코드 수정] --> L2[git add + commit]
        L2 --> L3[git push]
    end

    subgraph GITHUB["🐙 GitHub"]
        L3 --> G1[코드 저장소 업데이트]
    end

    subgraph RENDER["☁️ Render.com"]
        G1 -->|자동 감지| R1[빌드 시작]
        R1 -->|pip install| R2{빌드 성공?}
        R2 -->|실패| ERR_BUILD[❌ Deploy 에러<br/>Deploy 로그 확인]
        R2 -->|성공| R3[서버 시작<br/>uvicorn 실행]
        R3 -->|시작 실패| ERR_START[❌ 시작 에러<br/>Runtime 로그 확인]
        R3 -->|성공| R4[서비스 Live]
    end

    subgraph RUNTIME["🌐 운영 중"]
        R4 --> RT1[사용자 요청]
        RT1 --> RT2[FastAPI 처리]
        RT2 --> RT3[DB 통신]
        RT3 --> RT4[응답 반환]
        RT2 -->|에러 발생| ERR_RUNTIME[❌ Runtime 에러<br/>Runtime 로그 확인]
    end

    style ERR_BUILD fill:#ff6b6b,color:#fff
    style ERR_START fill:#ff6b6b,color:#fff
    style ERR_RUNTIME fill:#ff6b6b,color:#fff
```
